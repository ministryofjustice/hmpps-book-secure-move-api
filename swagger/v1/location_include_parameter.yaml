LocationIncludeParameter:
  name: include
  description: Returns a specific list of related resources to the location
  in: query
  style: form
  explode: false
  schema:
    type: string
    enum:
      - suppliers
    example: suppliers
