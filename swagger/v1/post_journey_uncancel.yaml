PostJourneyUncancel:
  type: object
  required:
  - type
  - attributes
  properties:
    type:
      type: string
      example: uncancel
      enum:
      - uncancel
      description: The type of this object - always `uncancel`
    attributes:
      type: object
      required:
      - timestamp
      properties:
        timestamp:
          $ref: timestamp_attribute.yaml#/Timestamp
        notes:
          $ref: notes_attribute.yaml#/Notes
