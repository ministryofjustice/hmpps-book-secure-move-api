ProfileIncludeParameter:
  name: include
  description: Returns a specific list of related resources to the profile
  in: query
  style: form
  explode: false
  schema:
    type: string
    enum:
      - documents
      - person
      - category
  example: documents
