GetComplete:
  type: object
  required:
  - type
  - attributes
  properties:
    id:
      type: string
      format: uuid
      example: d0911846-eb20-49dd-b4e1-9f65bcee4b7b
      description: The unique identifier (UUID) of this object
    type:
      type: string
      example: complete
      enum:
      - complete
      description: The type of this object - always `complete`
    attributes:
      type: object
      required:
      - timestamp
      properties:
        timestamp:
          $ref: timestamp_attribute.json#/Timestamp
PostComplete:
  type: object
  required:
  - type
  - attributes
  properties:
    type:
      type: string
      example: complete
      enum:
      - complete
      description: The type of this object - always `complete`
    attributes:
      type: object
      required:
      - timestamp
      properties:
        timestamp:
          $ref: timestamp_attribute.json#/Timestamp
