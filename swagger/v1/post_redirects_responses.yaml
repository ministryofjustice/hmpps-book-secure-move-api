'201':
  type: object
  required:
  - data
  properties:
    data:
      $ref: redirect.yaml#/GetRedirect
    included:
      type: array
      items:
        anyOf:
        - $ref: location.yaml#/Location
