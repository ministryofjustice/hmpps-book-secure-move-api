PersonIncludeParameter:
  name: include
  description: Returns a specific list of related resources to the person
  in: query
  style: form
  explode: false
  schema:
    type: string
    enum:
      - gender
      - ethnicity
  example: gender
