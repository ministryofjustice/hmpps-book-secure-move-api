PostMoveAccept:
  type: object
  required:
  - type
  - attributes
  properties:
    type:
      type: string
      example: approves
      enum:
      - approves
      description: The type of this object - always `approves`
    attributes:
      type: object
      required:
      - timestamp
      - date
      properties:
        timestamp:
          $ref: timestamp_attribute.yaml#/Timestamp
