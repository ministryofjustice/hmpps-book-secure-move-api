'201':
  type: object
  required:
  - data
  properties:
    data:
      $ref: lockout.yaml#/GetLockout
    included:
      type: array
      items:
        anyOf:
        - $ref: location.yaml#/Location
