'200':
  type: object
  required:
    - data
  properties:
    data:
      $ref: allocation.yaml#/Allocation
    included:
      type: array
      items:
        anyOf:
        - $ref: location.yaml#/Location
        - $ref: move.yaml#/Move
        - $ref: person.yaml#/Person
